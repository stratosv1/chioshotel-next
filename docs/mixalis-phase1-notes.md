# /mixalis Phase 1 notes

This phase intentionally does **not** create the `physics` database schema, upload storage, START generation, tests, or AI tutor. Those are separate validated phases defined in `docs/mixalis-BUILD.md`.

The chapter creation page is a UI skeleton only until persistence is added in Phase 2.
